<html style="font-size: 16px;" lang="en" class="u-responsive-xs">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <style>
      body {
          margin: 20px;
      }

      table {
          border-collapse: collapse;
          border: solid 2px gray;
      }

      td,
      th {
          padding: 5px;
      }

      th {
          background-color: #cde;
      }
  </style>
</head>
<body >

      <h4>รายการข้อมูล</h4>
          <table border ="1">
          <tr><th>ID</th><th>ชื่อสินค้า</th><th>จำนวนที่เหลือ</th><th>ผู้ดำเนินการ</th><th>วันที่ดำเนินการ</th></tr>
          <% if (locals.get_data) { %>
          <% for (key of get_data) {  
            let dt=new Date(Date.parse(key.Date)) 
            let df=`${dt.getDate()}-${dt.getMonth() + 1}-${dt.getFullYear()}`
          %>
              <tr>
                  <td><%= key.ID%></td>
                  <td><%= key.Product_Name%></td>
                  <td><%= key.Total%></td>
                  <td><%= key.Operator%></td>
                  <td><%= df%></td>
              </tr>
          <% } %>
        <% } %>
      </table>
</body>
<grammarly-desktop-integration data-grammarly-shadow-root="true"></grammarly-desktop-integration>

</html>